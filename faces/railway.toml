# Leave Root Directory as / (the repo root). Railway already sends `.` as context.
[build]
builder = "DOCKERFILE"
dockerfilePath = "faces/Dockerfile"
watchPatterns = ["/faces/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
